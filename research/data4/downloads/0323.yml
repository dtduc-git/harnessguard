name: Blog Generator

on:
  schedule:
    - cron: '0 9 * * 2'  # Tuesday 9am UTC / 4am CST (after scores at 3am)
  workflow_dispatch:

# Serialize every workflow that pushes to main so their push windows don't
# overlap. Combined with the rebase-retry push below, this eliminates the
# non-fast-forward rejections that emailed spurious "workflow failed" notices.
concurrency:
  group: content-push-main
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - run: npm ci

      # 1) Plan the article(s) (0-1) and emit a prompt file per article. Touches
      #    only Supabase — no model call. Sets job_count + model_<i> outputs.
      - name: Prepare blog jobs
        id: prep
        run: npx tsx bots/generate-blog.ts --prepare
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}

      # 2) Generate each planned article with the Claude Code action. This bills
      #    a Claude subscription via CLAUDE_CODE_OAUTH_TOKEN instead of Anthropic
      #    API credits — generate it locally with `claude setup-token` (Pro/Max)
      #    and store it as the CLAUDE_CODE_OAUTH_TOKEN repository secret.
      #
      #    Verify the secret with the Verify Claude Token workflow after every
      #    refresh. This step failed silently from 2026-06-25 to 2026-08-28
      #    because the stored secret was never a usable token — first 92 chars of
      #    something unrecognised, then a truncated one, then a full-length one
      #    with whitespace padding. Each produced a nine-second 401 and an empty
      #    blog, and nothing verified the value.
      #
      #    continue-on-error: the render fallback below is the whole point. A bad
      #    token must cost one plainer post, not another nine silent weeks.
      - name: Generate article 0 with Claude (subscription)
        if: steps.prep.outputs.job_count == '1' || steps.prep.outputs.job_count == '2'
        continue-on-error: true
        uses: anthropics/claude-code-base-action@main
        timeout-minutes: 15
        with:
          prompt_file: blog-work/prompt-0.txt
          claude_args: "--allowedTools Write --model ${{ steps.prep.outputs.model_0 }}"
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}

      - name: Generate article 1 with Claude (subscription)
        if: steps.prep.outputs.job_count == '2'
        continue-on-error: true
        uses: anthropics/claude-code-base-action@main
        timeout-minutes: 15
        with:
          prompt_file: blog-work/prompt-1.txt
          claude_args: "--allowedTools Write --model ${{ steps.prep.outputs.model_1 }}"
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}

      # 3) Turn the generated prose back into MDX posts and update .meta.json.
      #    Emits a `written` output so the fallback below can tell an empty model
      #    step from a successful one.
      - name: Finalize articles
        id: finalize
        if: steps.prep.outputs.job_count == '1' || steps.prep.outputs.job_count == '2'
        continue-on-error: true
        run: npx tsx bots/generate-blog.ts --finalize
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}

      # 4) Fallback. If the model wrote nothing — expired token, API outage,
      #    empty output — build the same planned articles from catalog data via
      #    bots/lib/blog-templates.ts. Plainer posts, but the blog ships.
      #    .meta.json dedup means anything the model DID write is not repeated.
      - name: Fall back to rendered articles
        id: fallback
        if: |
          steps.prep.outputs.job_count != '0' &&
          (steps.finalize.outcome != 'success' ||
           steps.finalize.outputs.written == '0' ||
           steps.finalize.outputs.written == '')
        run: |
          echo "::warning title=Blog fell back to templates::The Claude step produced no article. Run the Verify Claude Token workflow — the token is the usual cause."
          npx tsx bots/generate-blog.ts --render
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}

      - name: Commit and push new posts
        if: steps.prep.outputs.job_count == '1' || steps.prep.outputs.job_count == '2'
        run: |
          git config user.name "mcppedia-bot"
          git config user.email "bot@mcppedia.org"
          # data/blog-index.json is the frontmatter index that request-time
          # routes (/sitemap.xml, /llms.txt, /blog) read instead of walking
          # content/ — Cloudflare Workers have no filesystem. Regenerate it in
          # the same commit so the committed copy never lags the posts.
          node scripts/generate-content-index.mjs
          git add content/blog/ data/blog-index.json
          git diff --cached --quiet && echo "No new posts generated" && exit 0
          git commit -m "blog: auto-generate articles [bot]"
          # Rebase-retry: other bot workflows push to main too, so a push can be
          # rejected (non-fast-forward) if main moved since checkout. Rebase onto
          # the latest and retry instead of failing the job — the old plain
          # `git push` emailed a spurious failure even when the content was fine.
          for attempt in 1 2 3 4 5; do
            if git push; then
              echo "Pushed on attempt $attempt"
              exit 0
            fi
            echo "Push rejected (attempt $attempt) — rebasing onto origin/main"
            git pull --rebase --autostash origin main || true
          done
          echo "Push still failing after 5 attempts"
          exit 1

      # A green run that quietly shipped a template post is how this outage
      # stayed invisible. Make the degraded path loud in the run summary.
      - name: Report which path produced the posts
        if: always() && steps.prep.outputs.job_count != '0'
        run: |
          if [ "${{ steps.fallback.outcome }}" = "success" ]; then
            echo "### ⚠️ Blog published from TEMPLATES, not Claude" >> $GITHUB_STEP_SUMMARY
            echo "The model step produced nothing. Run **Verify Claude Token** — a stale or malformed CLAUDE_CODE_OAUTH_TOKEN is the usual cause." >> $GITHUB_STEP_SUMMARY
          else
            echo "### ✅ Blog published from Claude" >> $GITHUB_STEP_SUMMARY
            echo "Articles written: ${{ steps.finalize.outputs.written }}" >> $GITHUB_STEP_SUMMARY
          fi
