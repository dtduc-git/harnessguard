name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  workflow_dispatch:

jobs:
  unit-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12"]
    steps:
      - uses: actions/checkout@v6

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip setuptools wheel
          pip install -r requirements-ci.txt -r requirements-test.txt
          pip install pytest-timeout

      - name: Run unit tests with coverage
        if: matrix.python-version == '3.11'
        run: |
          python -m pytest tests/ -v --tb=short --timeout=120 \
            --cov=arkana --cov-report=xml \
            --cov-report=term-missing:skip-covered \
            --cov-config=.coveragerc \
            --cov-fail-under=65

      - name: Run unit tests
        if: matrix.python-version != '3.11'
        run: |
          python -m pytest tests/ -v --tb=short --timeout=120

      - name: Upload coverage report
        if: matrix.python-version == '3.11'
        uses: actions/upload-artifact@v7
        with:
          name: coverage-report
          path: coverage.xml

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install dependencies for syntax check
        run: |
          python -m pip install --upgrade pip setuptools wheel
          pip install -r requirements-ci.txt
          pip install ruff==0.9.10

      - name: Check syntax (all Python files)
        run: |
          python -c "
          import pathlib, py_compile, sys
          errors = []
          for p in sorted(pathlib.Path('arkana').rglob('*.py')):
              try:
                  py_compile.compile(str(p), doraise=True)
              except py_compile.PyCompileError as e:
                  errors.append(str(e))
          for p in sorted(pathlib.Path('tests').rglob('*.py')):
              try:
                  py_compile.compile(str(p), doraise=True)
              except py_compile.PyCompileError as e:
                  errors.append(str(e))
          if errors:
              print(f'{len(errors)} syntax error(s) found:')
              for e in errors:
                  print(e)
              sys.exit(1)
          else:
              print('All Python files passed syntax check.')
          "

      - name: Lint with ruff
        run: |
          ruff check arkana/ tests/ \
            --select=E9,F63,F7,F82,F841,W291,W292,W293,B006,B007,B018,UP031,UP032,RUF005,RUF010,RUF019,G010 \
            --no-fix

  smoke-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install minimal dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-ci.txt

      - name: Verify CLI --help works
        run: python -m arkana --help

      - name: Verify module imports
        run: |
          python -c "from arkana.constants import MAX_MCP_RESPONSE_SIZE_BYTES; print('constants OK')"
          python -c "from arkana.state import AnalyzerState; print('state OK')"
          python -c "from arkana.cache import AnalysisCache; print('cache OK')"

      - name: Validate Codex / MCP plugin manifests
        run: |
          python -c "
          import json
          import sys

          errors = []

          # 1. .codex-plugin/plugin.json — Codex CLI marketplace metadata
          with open('.codex-plugin/plugin.json') as f:
              codex = json.load(f)
          for required in ('name', 'version', 'description', 'repository', 'mcpServers'):
              if required not in codex:
                  errors.append(f'.codex-plugin/plugin.json missing required field: {required}')
          if codex.get('name') != 'Arkana':
              errors.append(f'.codex-plugin/plugin.json name must be \"Arkana\", got {codex.get(\"name\")!r}')
          if codex.get('mcpServers') != './.mcp.json':
              errors.append(f'.codex-plugin/plugin.json mcpServers must point at ./.mcp.json, got {codex.get(\"mcpServers\")!r}')

          # 2. .mcp.json — standard MCP client config (Claude Desktop / Codex / etc.)
          with open('.mcp.json') as f:
              mcp = json.load(f)
          servers = mcp.get('mcpServers') or {}
          if 'arkana' not in servers:
              errors.append('.mcp.json missing mcpServers.arkana entry')
          else:
              entry = servers['arkana']
              if not entry.get('command'):
                  errors.append('.mcp.json arkana entry missing command')
              if not isinstance(entry.get('args'), list):
                  errors.append('.mcp.json arkana entry args must be a list')

          if errors:
              for e in errors:
                  print(f'  - {e}', file=sys.stderr)
              sys.exit(1)
          print('Plugin manifests valid (.codex-plugin/plugin.json + .mcp.json)')
          "

  docker-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Build Docker image
        run: docker build -t arkana-test .
