name: Pipelock validation suite

on:
  pull_request:
    paths:
      - ".github/workflows/pipelock.yml"
      - "src/ai_multi_agent_platform/adapters/pipelock.py"
      - "tests/integration/security/test_pipelock_evaluation.py"
      - "tests/integration/security/test_pipelock_transports.py"
      - "tests/fixtures/mcp_*server.py"
      - "tests/integration/pipelock/**"
      - "upstream/pipelock-core.yaml"
      - "tests/integration/security/test_pipelock_security_corpus.py"
      - "tests/fixtures/pipelock_adversarial_cases.json"
      - "tests/fixtures/pipelock_websocket_audit.yaml"
      - "docs/upstream/PIPELOCK_CORE_*.md"
      - "tests/integration/security/test_pipelock_containment.py"
      - "tests/fixtures/mcp_stdio_network_probe_server.py"
      - "scripts/ci/inet_socket_deny_exec.py"
      - "tests/integration/security/test_pipelock_mcp_adversarial.py"
      - "tests/fixtures/mcp_adversarial_http_server.py"
      - "tests/fixtures/pipelock_mcp_adversarial.yaml"
      - "tests/integration/security/test_pipelock_network_exfiltration.py"
      - "tests/fixtures/pipelock_network_boundary_audit.yaml"
      - "tests/fixtures/pipelock_network_boundary_target.py"
      - "tests/integration/security/test_pipelock_outage_recovery.py"
      - "tests/fixtures/pipelock_outage_enforced.yaml"
      - "tests/fixtures/pipelock_outage_target_server.py"
      - "scripts/benchmarks/pipelock_benchmark.py"
      - "tests/fixtures/mcp_stdio_server.py"
      - "tests/fixtures/websocket_echo_server.py"
      - "tests/integration/files/test_pipelock_profile_bypass.py"
      - "tests/fixtures/pipelock_profile_bypass_target.py"
      - "tests/fixtures/websocket_adversarial_server.py"
      - "src/ai_multi_agent_platform/adapters/hermes.py"
      - "scripts/benchmarks/run_pipelock_vps_capture.sh"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  pinned-core-platform-compatibility:
    runs-on: ubuntu-latest
    steps:
      - name: Check out platform
        uses: actions/checkout@v7
      - name: Check out exact Pipelock Core candidate
        uses: actions/checkout@v7
        with:
          repository: luckyPipewrench/pipelock
          ref: f7d1816f1a5ad63d501b0c48f36066f836f59022
          path: .candidate/pipelock
          persist-credentials: false
      - name: Set up Go from pinned upstream module
        uses: actions/setup-go@v7
        with:
          go-version-file: .candidate/pipelock/go.mod
          cache-dependency-path: .candidate/pipelock/go.sum
      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.12"
          cache: pip
      - name: Install platform compatibility dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[dev]"
          python -m pip install "websockets==15.0.1"
      - name: Verify reviewed license and build boundaries
        shell: bash
        run: |
          set -euo pipefail
          grep -q "Apache License" .candidate/pipelock/LICENSE
          grep -q "Elastic License 2.0" .candidate/pipelock/enterprise/LICENSE
          grep -q '^//go:build enterprise$' .candidate/pipelock/enterprise/edition.go
          grep -q 'go build -tags enterprise' .candidate/pipelock/Dockerfile
          python - <<'PY'
          from pathlib import Path
          import re

          makefile = Path(".candidate/pipelock/Makefile").read_text(encoding="utf-8")
          build = makefile.split("\nbuild:\n", 1)[1].split("\n\n", 1)[0]
          if "-tags enterprise" in build:
              raise SystemExit("upstream default make build unexpectedly enables Enterprise tags")

          goreleaser = Path(".candidate/pipelock/.goreleaser.yaml").read_text(encoding="utf-8")
          release_build = re.search(
              r"(?ms)^  - id: pipelock\n(?P<body>.*?)(?=^  - id:|^archives:)",
              goreleaser,
          )
          if release_build is None or not re.search(
              r"(?m)^    tags:\n      - enterprise$", release_build.group("body")
          ):
              raise SystemExit("expected pinned GoReleaser pipelock build to be Enterprise-tagged")
          PY
      - name: Build tag-free Core candidate
        working-directory: .candidate/pipelock
        run: make build
      - name: Reject Enterprise-tagged candidate binaries
        working-directory: .candidate/pipelock
        shell: bash
        run: |
          set -euo pipefail
          go version -m ./pipelock | tee "$RUNNER_TEMP/pipelock-platform-compat-buildinfo.txt"
          if grep -Eq 'tags=.*enterprise' "$RUNNER_TEMP/pipelock-platform-compat-buildinfo.txt"; then
            echo "candidate binary contains Enterprise build tag" >&2
            exit 1
          fi
      - name: Generate audit-only evaluation configuration
        working-directory: .candidate/pipelock
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p "$RUNNER_TEMP/pipelock-platform-compat-home"
          HOME="$RUNNER_TEMP/pipelock-platform-compat-home" ./pipelock init \
            --scan-home "$RUNNER_TEMP/pipelock-platform-compat-home" \
            --output "$RUNNER_TEMP/pipelock-platform-compat-audit.yaml" \
            --preset audit \
            --skip-canary
          grep -Eq '^mode:[[:space:]]*audit$' "$RUNNER_TEMP/pipelock-platform-compat-audit.yaml"
          grep -Eq '^enforce:[[:space:]]*false$' "$RUNNER_TEMP/pipelock-platform-compat-audit.yaml"
          grep -Eq '^flight_recorder:' "$RUNNER_TEMP/pipelock-platform-compat-audit.yaml"
          grep -Eq '^[[:space:]]+redact:[[:space:]]*true$' "$RUNNER_TEMP/pipelock-platform-compat-audit.yaml"
          sha256sum ./pipelock "$RUNNER_TEMP/pipelock-platform-compat-audit.yaml" \
            > "$RUNNER_TEMP/pipelock-platform-compat-sha256.txt"
      - name: Exercise platform MCP stdio through pinned Pipelock
        env:
          PIPELOCK_TEST_BIN: ${{ github.workspace }}/.candidate/pipelock/pipelock
          PIPELOCK_TEST_CONFIG: ${{ runner.temp }}/pipelock-platform-compat-audit.yaml
        run: |
          pytest -q tests/integration/security/test_pipelock_evaluation.py \
            -k pipelock_mcp_stdio_wraps_canonical_invocation_path \
            --junitxml="$RUNNER_TEMP/pipelock-platform-mcp-stdio-junit.xml"
      - name: Exercise platform MCP HTTP and WebSocket upstreams through pinned Pipelock
        env:
          PIPELOCK_TEST_BIN: ${{ github.workspace }}/.candidate/pipelock/pipelock
          PIPELOCK_TEST_CONFIG: ${{ runner.temp }}/pipelock-platform-compat-audit.yaml
        run: |
          pytest -q tests/integration/security/test_pipelock_transports.py \
            --junitxml="$RUNNER_TEMP/pipelock-platform-mcp-remote-junit.xml"
      - name: Exercise retained adversarial Pipelock integration cases
        env:
          PIPELOCK_TEST_BIN: ${{ github.workspace }}/.candidate/pipelock/pipelock
          PIPELOCK_TEST_CONFIG: ${{ runner.temp }}/pipelock-platform-compat-audit.yaml
        run: |
          pytest -q tests/integration/pipelock/test_adversarial_security.py \
            --junitxml="$RUNNER_TEMP/pipelock-platform-adversarial-junit.xml"
      - name: Exercise audit-only HTTP fetch path
        working-directory: .candidate/pipelock
        shell: bash
        run: |
          set -euo pipefail
          HOME="$RUNNER_TEMP/pipelock-platform-compat-home" ./pipelock run \
            --config "$RUNNER_TEMP/pipelock-platform-compat-audit.yaml" \
            > "$RUNNER_TEMP/pipelock-platform-audit.log" 2>&1 &
          pipelock_pid=$!
          trap 'kill "$pipelock_pid" 2>/dev/null || true' EXIT

          success=0
          for _ in $(seq 1 20); do
            if curl --fail --silent --show-error --max-time 20 \
              --get --data-urlencode 'url=https://example.com/' \
              --output "$RUNNER_TEMP/pipelock-platform-fetch-output.txt" \
              --write-out '%{time_total}\n' \
              http://127.0.0.1:8888/fetch \
              > "$RUNNER_TEMP/pipelock-platform-fetch-latency-seconds.txt"; then
              success=1
              break
            fi
            sleep 1
          done
          test "$success" -eq 1
          test -s "$RUNNER_TEMP/pipelock-platform-fetch-output.txt"
      - name: Exercise HTTP forward and HTTPS CONNECT paths
        working-directory: .candidate/pipelock
        shell: bash
        run: |
          set -euo pipefail
          cp "$RUNNER_TEMP/pipelock-platform-compat-audit.yaml" "$RUNNER_TEMP/pipelock-platform-forward.yaml"
          python - <<'PY'
          from pathlib import Path
          import os
          import re

          path = Path(os.environ["RUNNER_TEMP"]) / "pipelock-platform-forward.yaml"
          lines = path.read_text(encoding="utf-8").splitlines(keepends=True)
          in_forward = False
          changed = False
          for index, line in enumerate(lines):
              if line.startswith("forward_proxy:"):
                  in_forward = True
                  continue
              if in_forward and line.strip() and not line.startswith((" ", "\t", "#")):
                  break
              if in_forward and re.match(r"^[ \t]+enabled:[ \t]*(?:true|false)[ \t]*$", line.rstrip("\n")):
                  indent = line[: len(line) - len(line.lstrip())]
                  lines[index] = f"{indent}enabled: true\n"
                  changed = True
                  break
          if not changed:
              raise SystemExit("generated audit config has no forward_proxy.enabled field")
          path.write_text("".join(lines), encoding="utf-8")
          PY
          grep -A4 '^forward_proxy:' "$RUNNER_TEMP/pipelock-platform-forward.yaml" \
            | grep -Eq '^[[:space:]]+enabled:[[:space:]]*true$'
          sha256sum "$RUNNER_TEMP/pipelock-platform-forward.yaml" \
            > "$RUNNER_TEMP/pipelock-platform-forward-config-sha256.txt"

          HOME="$RUNNER_TEMP/pipelock-platform-compat-home" ./pipelock run \
            --config "$RUNNER_TEMP/pipelock-platform-forward.yaml" \
            > "$RUNNER_TEMP/pipelock-platform-forward-connect.log" 2>&1 &
          pipelock_pid=$!
          trap 'kill "$pipelock_pid" 2>/dev/null || true' EXIT

          success=0
          for _ in $(seq 1 20); do
            if curl --fail --silent --show-error --max-time 20 \
              --proxy http://127.0.0.1:8888 --noproxy "" \
              --output "$RUNNER_TEMP/pipelock-platform-forward-output.txt" \
              --write-out '%{time_total}\n' \
              http://example.com/ \
              > "$RUNNER_TEMP/pipelock-platform-forward-latency-seconds.txt"; then
              success=1
              break
            fi
            sleep 1
          done
          test "$success" -eq 1
          test -s "$RUNNER_TEMP/pipelock-platform-forward-output.txt"

          curl --fail --silent --show-error --max-time 20 \
            --proxy http://127.0.0.1:8888 --noproxy "" \
            --output "$RUNNER_TEMP/pipelock-platform-connect-output.txt" \
            --write-out '%{time_total}\n' \
            https://example.com/ \
            > "$RUNNER_TEMP/pipelock-platform-connect-latency-seconds.txt"
          test -s "$RUNNER_TEMP/pipelock-platform-connect-output.txt"
      - name: Upload pinned platform compatibility evidence
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: pipelock-platform-compatibility-evidence
          path: |
            ${{ runner.temp }}/pipelock-platform-compat-buildinfo.txt
            ${{ runner.temp }}/pipelock-platform-compat-sha256.txt
            ${{ runner.temp }}/pipelock-platform-mcp-stdio-junit.xml
            ${{ runner.temp }}/pipelock-platform-mcp-remote-junit.xml
            ${{ runner.temp }}/pipelock-platform-adversarial-junit.xml
            ${{ runner.temp }}/pipelock-platform-fetch-latency-seconds.txt
            ${{ runner.temp }}/pipelock-platform-forward-latency-seconds.txt
            ${{ runner.temp }}/pipelock-platform-connect-latency-seconds.txt
            ${{ runner.temp }}/pipelock-platform-forward-config-sha256.txt
            ${{ runner.temp }}/pipelock-platform-audit.log
            ${{ runner.temp }}/pipelock-platform-forward-connect.log
          if-no-files-found: error

  pinned-core-adversarial-corpus:
    runs-on: ubuntu-latest
    steps:
      - name: Check out platform
        uses: actions/checkout@v7
      - name: Check out exact Pipelock Core candidate
        uses: actions/checkout@v7
        with:
          repository: luckyPipewrench/pipelock
          ref: f7d1816f1a5ad63d501b0c48f36066f836f59022
          path: .candidate/pipelock
          persist-credentials: false
      - name: Set up Go from pinned upstream module
        uses: actions/setup-go@v7
        with:
          go-version-file: .candidate/pipelock/go.mod
          cache-dependency-path: .candidate/pipelock/go.sum
      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.12"
          cache: pip
      - name: Install platform test dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[dev]"
          python -m pip install "websockets==15.0.1"
      - name: Build tag-free Core candidate
        working-directory: .candidate/pipelock
        run: make build
      - name: Reject Enterprise-tagged candidate binaries
        working-directory: .candidate/pipelock
        shell: bash
        run: |
          set -euo pipefail
          go version -m ./pipelock | tee "$RUNNER_TEMP/pipelock-adversarial-buildinfo.txt"
          if grep -Eq 'tags=.*enterprise' "$RUNNER_TEMP/pipelock-adversarial-buildinfo.txt"; then
            echo "candidate binary contains Enterprise build tag" >&2
            exit 1
          fi
      - name: Exercise adversarial security corpus
        env:
          PIPELOCK_TEST_BIN: ${{ github.workspace }}/.candidate/pipelock/pipelock
        run: |
          pytest -q tests/integration/security/test_pipelock_security_corpus.py \
            --basetemp="$RUNNER_TEMP/pipelock-security" \
            --junitxml="$RUNNER_TEMP/pipelock-security-junit.xml"
      - name: Upload synthetic adversarial evidence
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: pipelock-adversarial-corpus-evidence
          path: |
            ${{ runner.temp }}/pipelock-adversarial-buildinfo.txt
            ${{ runner.temp }}/pipelock-security-junit.xml
            ${{ runner.temp }}/pipelock-security/**/*.log
          if-no-files-found: warn

  pinned-core-containment-bypass:
    runs-on: ubuntu-latest
    steps:
      - name: Check out platform
        uses: actions/checkout@v7
      - name: Check out exact Pipelock Core candidate
        uses: actions/checkout@v7
        with:
          repository: luckyPipewrench/pipelock
          ref: f7d1816f1a5ad63d501b0c48f36066f836f59022
          path: .candidate/pipelock
          persist-credentials: false
      - name: Set up Go from pinned upstream module
        uses: actions/setup-go@v7
        with:
          go-version-file: .candidate/pipelock/go.mod
          cache-dependency-path: .candidate/pipelock/go.sum
      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.12"
          cache: pip
      - name: Install platform test dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[dev]"
      - name: Build tag-free Core candidate
        working-directory: .candidate/pipelock
        run: make build
      - name: Reject Enterprise-tagged candidate binaries
        working-directory: .candidate/pipelock
        shell: bash
        run: |
          set -euo pipefail
          go version -m ./pipelock | tee "$RUNNER_TEMP/pipelock-containment-buildinfo.txt"
          if grep -Eq 'tags=.*enterprise' "$RUNNER_TEMP/pipelock-containment-buildinfo.txt"; then
            echo "candidate binary contains Enterprise build tag" >&2
            exit 1
          fi
      - name: Generate audit-only evaluation configuration
        working-directory: .candidate/pipelock
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p "$RUNNER_TEMP/pipelock-home"
          HOME="$RUNNER_TEMP/pipelock-home" ./pipelock init \
            --scan-home "$RUNNER_TEMP/pipelock-home" \
            --output "$RUNNER_TEMP/pipelock-audit.yaml" \
            --preset audit \
            --skip-canary
          grep -Eq '^mode:[[:space:]]*audit$' "$RUNNER_TEMP/pipelock-audit.yaml"
          sha256sum ./pipelock "$RUNNER_TEMP/pipelock-audit.yaml" \
            > "$RUNNER_TEMP/pipelock-containment-sha256.txt"
      - name: Exercise direct-bypass and contained MCP stdio profiles
        env:
          PIPELOCK_TEST_BIN: ${{ github.workspace }}/.candidate/pipelock/pipelock
          PIPELOCK_TEST_CONFIG: ${{ runner.temp }}/pipelock-audit.yaml
        run: |
          pytest -q tests/integration/security/test_pipelock_containment.py \
            --junitxml="$RUNNER_TEMP/pipelock-containment-junit.xml"
      - name: Upload containment evidence
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: pipelock-containment-bypass-evidence
          path: |
            ${{ runner.temp }}/pipelock-containment-buildinfo.txt
            ${{ runner.temp }}/pipelock-containment-sha256.txt
            ${{ runner.temp }}/pipelock-containment-junit.xml
          if-no-files-found: error

  pinned-core-mcp-adversarial:
    runs-on: ubuntu-latest
    steps:
      - name: Check out platform
        uses: actions/checkout@v7
      - name: Check out exact Pipelock Core candidate
        uses: actions/checkout@v7
        with:
          repository: luckyPipewrench/pipelock
          ref: f7d1816f1a5ad63d501b0c48f36066f836f59022
          path: .candidate/pipelock
          persist-credentials: false
      - name: Set up Go from pinned upstream module
        uses: actions/setup-go@v7
        with:
          go-version-file: .candidate/pipelock/go.mod
          cache-dependency-path: .candidate/pipelock/go.sum
      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.12"
          cache: pip
      - name: Install platform test dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[dev]"
      - name: Build tag-free Core candidate
        working-directory: .candidate/pipelock
        run: make build
      - name: Reject Enterprise-tagged candidate binaries
        working-directory: .candidate/pipelock
        shell: bash
        run: |
          set -euo pipefail
          go version -m ./pipelock | tee "$RUNNER_TEMP/pipelock-mcp-adversarial-buildinfo.txt"
          if grep -Eq 'tags=.*enterprise' "$RUNNER_TEMP/pipelock-mcp-adversarial-buildinfo.txt"; then
            echo "candidate binary contains Enterprise build tag" >&2
            exit 1
          fi
      - name: Exercise MCP adversarial cases
        env:
          PIPELOCK_TEST_BIN: ${{ github.workspace }}/.candidate/pipelock/pipelock
        run: |
          pytest -q tests/integration/security/test_pipelock_mcp_adversarial.py \
            --basetemp="$RUNNER_TEMP/pipelock-mcp-adversarial" \
            --junitxml="$RUNNER_TEMP/pipelock-mcp-adversarial-junit.xml"
      - name: Upload synthetic MCP adversarial evidence
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: pipelock-mcp-adversarial-evidence
          path: |
            ${{ runner.temp }}/pipelock-mcp-adversarial-buildinfo.txt
            ${{ runner.temp }}/pipelock-mcp-adversarial-junit.xml
            ${{ runner.temp }}/pipelock-mcp-adversarial/**/*.log
          if-no-files-found: warn

  pinned-core-network-exfiltration:
    runs-on: ubuntu-latest
    steps:
      - name: Check out platform
        uses: actions/checkout@v7
      - name: Check out exact Pipelock Core candidate
        uses: actions/checkout@v7
        with:
          repository: luckyPipewrench/pipelock
          ref: f7d1816f1a5ad63d501b0c48f36066f836f59022
          path: .candidate/pipelock
          persist-credentials: false
      - name: Set up Go from pinned upstream module
        uses: actions/setup-go@v7
        with:
          go-version-file: .candidate/pipelock/go.mod
          cache-dependency-path: .candidate/pipelock/go.sum
      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.12"
          cache: pip
      - name: Install platform test dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[dev]"
      - name: Build tag-free Core candidate
        working-directory: .candidate/pipelock
        run: make build
      - name: Reject Enterprise-tagged candidate binaries
        working-directory: .candidate/pipelock
        shell: bash
        run: |
          set -euo pipefail
          go version -m ./pipelock | tee "$RUNNER_TEMP/pipelock-network-buildinfo.txt"
          if grep -Eq 'tags=.*enterprise' "$RUNNER_TEMP/pipelock-network-buildinfo.txt"; then
            echo "candidate binary contains Enterprise build tag" >&2
            exit 1
          fi
      - name: Add deterministic TEST-NET target address
        run: sudo ip address add 192.0.2.123/32 dev lo
      - name: Exercise network and multi-stage exfiltration cases
        env:
          PIPELOCK_TEST_BIN: ${{ github.workspace }}/.candidate/pipelock/pipelock
          PIPELOCK_ALLOW_HOSTS_MUTATION: "1"
          PIPELOCK_TEST_NET_IP: "192.0.2.123"
        run: |
          pytest -q tests/integration/security/test_pipelock_network_exfiltration.py \
            --basetemp="$RUNNER_TEMP/pipelock-network" \
            --junitxml="$RUNNER_TEMP/pipelock-network-junit.xml"
      - name: Remove deterministic TEST-NET target address
        if: always()
        run: sudo ip address del 192.0.2.123/32 dev lo || true
      - name: Upload network and exfiltration evidence
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: pipelock-network-exfiltration-evidence
          path: |
            ${{ runner.temp }}/pipelock-network-buildinfo.txt
            ${{ runner.temp }}/pipelock-network-junit.xml
            ${{ runner.temp }}/pipelock-network/**/*.log
            ${{ runner.temp }}/pipelock-network/**/*.jsonl
          if-no-files-found: warn

  pinned-core-outage-recovery:
    runs-on: ubuntu-latest
    steps:
      - name: Check out platform
        uses: actions/checkout@v7
      - name: Check out exact Pipelock Core candidate
        uses: actions/checkout@v7
        with:
          repository: luckyPipewrench/pipelock
          ref: f7d1816f1a5ad63d501b0c48f36066f836f59022
          path: .candidate/pipelock
          persist-credentials: false
      - name: Set up Go from pinned upstream module
        uses: actions/setup-go@v7
        with:
          go-version-file: .candidate/pipelock/go.mod
          cache-dependency-path: .candidate/pipelock/go.sum
      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.12"
          cache: pip
      - name: Install platform test dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[dev]"
      - name: Build tag-free Core candidate
        working-directory: .candidate/pipelock
        run: make build
      - name: Reject Enterprise-tagged candidate binaries
        working-directory: .candidate/pipelock
        shell: bash
        run: |
          set -euo pipefail
          go version -m ./pipelock | tee "$RUNNER_TEMP/pipelock-outage-buildinfo.txt"
          if grep -Eq 'tags=.*enterprise' "$RUNNER_TEMP/pipelock-outage-buildinfo.txt"; then
            echo "candidate binary contains Enterprise build tag" >&2
            exit 1
          fi
      - name: Exercise enforced outage and recovery
        env:
          PIPELOCK_TEST_BIN: ${{ github.workspace }}/.candidate/pipelock/pipelock
        run: |
          pytest -q tests/integration/security/test_pipelock_outage_recovery.py \
            --basetemp="$RUNNER_TEMP/pipelock-outage" \
            --junitxml="$RUNNER_TEMP/pipelock-outage-junit.xml"
      - name: Upload outage and recovery evidence
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: pipelock-outage-recovery-evidence
          path: |
            ${{ runner.temp }}/pipelock-outage-buildinfo.txt
            ${{ runner.temp }}/pipelock-outage-junit.xml
            ${{ runner.temp }}/pipelock-outage/**/*.log
            ${{ runner.temp }}/pipelock-outage/**/target-count.txt
          if-no-files-found: warn

  pinned-core-performance-reference:
    runs-on: ubuntu-latest
    steps:
      - name: Check out platform
        uses: actions/checkout@v7
      - name: Check out exact Pipelock Core candidate
        uses: actions/checkout@v7
        with:
          repository: luckyPipewrench/pipelock
          ref: f7d1816f1a5ad63d501b0c48f36066f836f59022
          path: .candidate/pipelock
          persist-credentials: false
      - name: Set up Go from pinned upstream module
        uses: actions/setup-go@v7
        with:
          go-version-file: .candidate/pipelock/go.mod
          cache-dependency-path: .candidate/pipelock/go.sum
      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.12"
          cache: pip
      - name: Install platform benchmark dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[dev]"
          python -m pip install "websockets==15.0.1"
      - name: Build tag-free Core candidate
        working-directory: .candidate/pipelock
        run: make build
      - name: Reject Enterprise-tagged candidate binaries
        working-directory: .candidate/pipelock
        shell: bash
        run: |
          set -euo pipefail
          go version -m ./pipelock | tee "$RUNNER_TEMP/pipelock-performance-buildinfo.txt"
          if grep -Eq 'tags=.*enterprise' "$RUNNER_TEMP/pipelock-performance-buildinfo.txt"; then
            echo "candidate binary contains Enterprise build tag" >&2
            exit 1
          fi
          sha256sum ./pipelock \
            "$GITHUB_WORKSPACE/tests/fixtures/pipelock_websocket_audit.yaml" \
            > "$RUNNER_TEMP/pipelock-performance-sha256.txt"
      - name: Run hosted-runner reference benchmark
        run: |
          python scripts/benchmarks/pipelock_benchmark.py \
            --pipelock-bin "$GITHUB_WORKSPACE/.candidate/pipelock/pipelock" \
            --config "$GITHUB_WORKSPACE/tests/fixtures/pipelock_websocket_audit.yaml" \
            --output "$RUNNER_TEMP/pipelock-performance-reference.json" \
            --environment-label "github-hosted-ubuntu-reference" \
            --iterations 20 \
            --warmups 3 \
            --mcp-iterations 5
      - name: Validate benchmark evidence contract
        shell: bash
        run: |
          python - <<'PY'
          import json
          import os
          from pathlib import Path
          result = json.loads((Path(os.environ["RUNNER_TEMP"]) / "pipelock-performance-reference.json").read_text(encoding="utf-8"))
          assert result["schema_version"] == 2
          assert result["evidence_suite"] == "pipelock-core-evaluation"
          assert result["environment"]["label"] == "github-hosted-ubuntu-reference"
          assert result["measurement_scope"]["hosted_runner_is_vps_evidence"] is False
          for surface in ("http", "websocket_roundtrip", "mcp_stdio_call_total"):
              assert result["latency"][surface]["direct"]["count"] > 0
              assert result["latency"][surface]["mediated"]["count"] > 0
          for key in (
              "pipelock_run_startup_ms",
              "pipelock_rss_peak_observed_bytes",
              "pipelock_home_and_stdout_growth_bytes",
          ):
              assert result["operability"][key] >= 0
          classification = result["classification"]
          assert classification["true_positive"] + classification["false_negative"] > 0
          assert classification["true_negative"] + classification["false_positive"] > 0
          PY
      - name: Upload reference performance evidence
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: pipelock-performance-reference-evidence
          path: |
            ${{ runner.temp }}/pipelock-performance-buildinfo.txt
            ${{ runner.temp }}/pipelock-performance-sha256.txt
            ${{ runner.temp }}/pipelock-performance-reference.json
            ${{ runner.temp }}/pipelock-benchmark-work/pipelock.log
          if-no-files-found: warn

  pinned-core-profile-bypass:
    runs-on: ubuntu-latest
    steps:
      - name: Check out platform
        uses: actions/checkout@v7
      - name: Check out exact Pipelock Core candidate
        uses: actions/checkout@v7
        with:
          repository: luckyPipewrench/pipelock
          ref: f7d1816f1a5ad63d501b0c48f36066f836f59022
          path: .candidate/pipelock
          persist-credentials: false
      - name: Set up Go from pinned upstream module
        uses: actions/setup-go@v7
        with:
          go-version-file: .candidate/pipelock/go.mod
          cache-dependency-path: .candidate/pipelock/go.sum
      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.12"
          cache: pip
      - name: Install platform test dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[dev]"
          python -m pip install "websockets==15.0.1"
      - name: Build tag-free Core candidate
        working-directory: .candidate/pipelock
        run: make build
      - name: Reject Enterprise-tagged candidate binaries
        working-directory: .candidate/pipelock
        shell: bash
        run: |
          set -euo pipefail
          go version -m ./pipelock | tee "$RUNNER_TEMP/pipelock-profile-bypass-buildinfo.txt"
          if grep -Eq 'tags=.*enterprise' "$RUNNER_TEMP/pipelock-profile-bypass-buildinfo.txt"; then
            echo "candidate binary contains Enterprise build tag" >&2
            exit 1
          fi
          sha256sum ./pipelock \
            "$GITHUB_WORKSPACE/tests/fixtures/pipelock_websocket_audit.yaml" \
            > "$RUNNER_TEMP/pipelock-profile-bypass-sha256.txt"
      - name: Exercise direct application and Hermes bypass paths
        env:
          PIPELOCK_TEST_BIN: ${{ github.workspace }}/.candidate/pipelock/pipelock
        run: |
          pytest -q tests/integration/files/test_pipelock_profile_bypass.py \
            --basetemp="$RUNNER_TEMP/pipelock-profile-bypass" \
            --junitxml="$RUNNER_TEMP/pipelock-profile-bypass-junit.xml"
      - name: Upload profile-bypass evidence
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: pipelock-profile-bypass-evidence
          path: |
            ${{ runner.temp }}/pipelock-profile-bypass-buildinfo.txt
            ${{ runner.temp }}/pipelock-profile-bypass-sha256.txt
            ${{ runner.temp }}/pipelock-profile-bypass-junit.xml
            ${{ runner.temp }}/pipelock-profile-bypass/**/*.log
            ${{ runner.temp }}/pipelock-profile-bypass/**/*.jsonl
            ${{ runner.temp }}/pipelock-profile-bypass/**/*.txt
          if-no-files-found: warn

  validate-capture-contract:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Validate shell syntax and help path
        run: |
          bash -n scripts/benchmarks/run_pipelock_vps_capture.sh
          bash scripts/benchmarks/run_pipelock_vps_capture.sh --help
      - name: Verify evidence pin and hosted-runner refusal
        shell: bash
        run: |
          set -euo pipefail
          grep -F 'f7d1816f1a5ad63d501b0c48f36066f836f59022' \
            scripts/benchmarks/run_pipelock_vps_capture.sh
          set +e
          GITHUB_ACTIONS=true bash scripts/benchmarks/run_pipelock_vps_capture.sh \
            "$RUNNER_TEMP/should-not-run" >"$RUNNER_TEMP/refusal.log" 2>&1
          status=$?
          set -e
          test "$status" -eq 2
          grep -F 'refusing to label a GitHub Actions runner as ordinary-VPS evidence' \
            "$RUNNER_TEMP/refusal.log"
