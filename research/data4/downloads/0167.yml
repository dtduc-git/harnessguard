name: Agent Tools CI

# Runs the previously-orphaned test suites for the three shipped
# agent-integration surfaces (tech-debt audit 2026-06-11, finding H6):
#   - tools/openburnbar-mcp        (local MCP server, pytest)
#   - tools/openburnbar-mcp-remote (mcp-remote bridge, node --test via npm test)
#   - quota-runner                 (hosted quota probe, node --test)
#
# Before this workflow these ~95 passing tests ran in zero CI lanes, so a
# regression in any of them shipped green. Each job is path-filtered to its own
# surface so the workflow only runs when the relevant code changes, and every
# job is BLOCKING (no continue-on-error) — a real failure fails the PR.

on:
  pull_request:
    branches: [main]
    paths:
      - "tools/openburnbar-mcp/**"
      - "tools/openburnbar-mcp-remote/**"
      - "quota-runner/**"
      - ".github/workflows/agent-tools-ci.yml"
  merge_group:
    branches: [main]
    types: [checks_requested]

permissions:
  contents: read

concurrency:
  group: agent-tools-ci-${{ github.event_name }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  # ── Local MCP server: pytest ──────────────────────────────────────────────
  openburnbar-mcp:
    name: openburnbar-mcp (pytest)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5.0.1 (node24-native; June 16 forced-runtime fuse)

      - uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v5
        with:
          python-version: "3.11"

      # This intentionally runs before Rust is installed or the native domain
      # core is built: the repo-scoped memory MCP must stand up with Python alone.
      - name: Verify cargo-free memory bootstrap
        run: tools/openburnbar-mcp/bootstrap-memory.test.sh

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@b3b07ba8b418998c39fb20f53e8b695cdcc8de1b # v1
        with:
          toolchain: "1.96.0" # pin: crates/openburnbar-iroh (libsignal lineage)

      - name: Install dependencies (incl. test runner)
        working-directory: tools/openburnbar-mcp
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-dev.txt

      - name: Build packaged Python domain core
        run: ./scripts/build-domain-core-python.sh

      - name: Run pytest
        working-directory: tools/openburnbar-mcp
        run: pytest tests/ -q

  # ── mcp-remote bridge: node --test (via npm test) ─────────────────────────
  openburnbar-mcp-remote:
    name: openburnbar-mcp-remote (node --test)
    runs-on: ubuntu-latest
    # 15 rather than 8: a full clone of this repo's 8.6 GiB history consumed the
    # entire budget and cancelled this job inside actions/checkout on a
    # merge_group ref with zero test work done (run 31041261000: 8m01s in
    # checkout). The sparse fetch below is the real remedy.
    timeout-minutes: 15
    steps:
      # Everything this job installs and tests lives under the one tool
      # directory referenced by every step below.
      - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5.0.1 (node24-native; June 16 forced-runtime fuse)
        with:
          filter: blob:none
          sparse-checkout: tools/openburnbar-mcp-remote

      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0 (node24-native)
        with:
          node-version-file: .nvmrc
          cache: npm
          cache-dependency-path: tools/openburnbar-mcp-remote/package-lock.json

      - name: Install dependencies
        run: npm ci --prefix tools/openburnbar-mcp-remote

      - name: Test (build + node --test)
        run: npm --prefix tools/openburnbar-mcp-remote test

  # ── quota-runner hosted probe: node --test ────────────────────────────────
  quota-runner:
    name: quota-runner (node --test)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5.0.1 (node24-native; June 16 forced-runtime fuse)

      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0 (node24-native)
        with:
          node-version-file: .nvmrc
          cache: npm
          cache-dependency-path: quota-runner/package-lock.json

      - name: Install dependencies
        run: npm ci --prefix quota-runner

      - name: Test (node --test)
        run: npm --prefix quota-runner test

  # ── Blocking gate (H6) ────────────────────────────────────────────────────
  # The three jobs above are path-filtered, so on most PRs they're skipped and
  # only the changed surface runs. A required status check that's sometimes
  # skipped can't block, and a job that merely runs isn't a gate unless its
  # failure fails the PR. This summary job (if: always(), mirrors
  # fast-feedback-gate) is the single BLOCKING check: it fails when any test job
  # ended in anything other than success or skipped, so a real test failure
  # turns the PR red instead of staying advisory.
  agent-tools-gate:
    name: Agent Tools Gate
    needs:
      - openburnbar-mcp
      - openburnbar-mcp-remote
      - quota-runner
    runs-on: ubuntu-latest
    if: always()
    steps:
      - name: Check all agent-tools jobs passed
        run: |
          results='${{ toJSON(needs) }}'
          failed=$(echo "$results" | python3 -c "
          import json, sys
          data = json.load(sys.stdin)
          failed = [k for k, v in data.items() if v.get('result') not in ('success', 'skipped')]
          print('\n'.join(failed))
          ")
          if [[ -n "$failed" ]]; then
            echo "::error::The following agent-tools jobs failed:"
            echo "$failed"
            exit 1
          fi
          echo "### Agent Tools CI: all surfaces passed" >> "$GITHUB_STEP_SUMMARY"
